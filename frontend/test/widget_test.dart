import 'package:flutter_test/flutter_test.dart';

import 'package:appsecurity/main.dart';

void main() {
  testWidgets('AppSecurityApp builds', (WidgetTester tester) async {
    expect(AppSecurityApp, isA<Object>());
  });
}